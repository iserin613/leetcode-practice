## 最近公共祖先

# DFS遍历
思路类似 `前序遍历` 
可能情形分为两种
- p q分别位于一个节点延伸出的左右枝上
- 一个节点是另一个节点的祖先之一

进行中左右遍历时 如果根节点就是p或q 直接返回root
如果越过叶子节点 返回null

如果dfs（root->left）非空 则说明左边子树存在节点
如果右边也非空 则由于进行了深度优先搜索 该根节点就是最终最近公共祖先
如果右边为空 则要么深度太深需要回溯 要么另一个节点在同一支上 同样返回left到上一级